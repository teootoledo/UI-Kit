import React from "react";
import { Headline } from "../components/UI-Kit/Utils/Texts";

interface Props {}

export const About = (props: Props) => {
  return (
    <div>
      <Headline text="About" />
    </div>
  );
};
